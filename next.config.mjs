/** @type {import('next').NextConfig} */
const nextConfig = {

    // next.config.js
    images: {
      domains: ['images.unsplash.com','ih0.redbubble.net'],
      
    },
  
};

export default nextConfig;
